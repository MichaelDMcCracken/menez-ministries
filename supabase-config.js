/**
 * Supabase project configuration.
 * Replace the placeholder values below with your actual Supabase project credentials.
 *
 * SUPABASE_URL  – found at: Project Settings → API → Project URL
 * SUPABASE_ANON_KEY – found at: Project Settings → API → anon / public key
 *
 * Both values are safe to expose in client-side code; Row Level Security (RLS)
 * controls what anonymous visitors and authenticated admins can do.
 */

// eslint-disable-next-line no-unused-vars
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
// eslint-disable-next-line no-unused-vars
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
